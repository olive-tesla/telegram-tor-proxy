# 🧅 Telegram Tor Proxy Manager

[![Python Version](https://img.shields.io/badge/python-3.8%2B-blue)](https://python.org)
[![Platform](https://img.shields.io/badge/platform-Windows-lightgrey)]()
---
# 📌 Что это и для чего?

*Кратко*: Данный скрипт на выходе даёт бесплатный **SOCKS5** прокси, используя соединение через сеть Tor

Подойдёт для **ускорения** соединения (например для **Telegram**)

Процесс практически полностью автоматизирован - в пару кликов можно пользоваться Telegram используя прокси.  
Также возможно использовать функционал этого скрипта и для других приложений

Что такое Tor и как работает его сеть, Если необходимо - можете ознакомиться в интернете.


Скрипт разворачивает локальный Tor‑сервер с SOCKS5‑прокси на порту 9090 и автоматически предлагает Telegram  
добавить этот прокси в список используемых. После первичной настройки вы получаете файл `tor-proxy.bat` для удобного   
запуска скрипта — он поднимает Tor‑туннель, и позволяет Telegram (и не только*) работать через сеть Tor.

## 🚀 Быстрый старт

Всё что нужно - запустить `start_proxy.bat` (или `main.py`) и дождаться загрузки Tor Expert Bundle и первичной настройки.  
(опционально) Добавить ваши мосты в сгенерированный после первого запуска `torrc` файл. Всё готово.
Теперь, когда вам нужно использовать ваш прокси, снова запустите скрипт через `start_proxy.bat`,  
дождитесь построения tor цепочки, поймёте по выводу в консоль "Bootstrapped 100%" - значит всё уже работает.
Теперь вы можете использовать `SOCKS5` прокси `127.0.0.1:9090` для подключения. 

### 🔧 Системные требования

    Windows 10 / 11 (на других системах не проверялось)

    Установленный Python версии 3.14 или выше
    При установке отметьте галочку «Add Python to PATH».  
    Интернет.

## 📥 Установка и первый запуск
- Скачайте ZIP‑архив проекта (сверху-справа зелёная кнопка `Code`-`Download ZIP`) и распакуйте в удобное место
- Или клонируйте репозиторий, пример команды для Git:  

`git clone https://github.com/olive-tesla/telegram_tor_proxy_manager.git`

- Запустите `start_proxy.bat` (Дважды кликните по файлу) Дальнейшие инструкции ориентированы на этот метод.

- Либо любым другим удобным вам способом. Входная точка у скрипта - `main.py`  

Откроется консольное окно, отображающее процесс установки.

    Если папка tor отсутствует, скрипт скачает Tor Expert Bundle (≈ 20–40 МБ).

    После загрузки откроется ссылка tg://socks... — Telegram предложит добавить прокси. 
    Нажмите «Подключиться».  
    Либо можете добавить вручную, разницы нет.

Добавьте мосты в `torrc` !!!!!!
   
   В корне проекта появится файл `torrc`. Откройте его любым текстовым редактором (Notepad++, да хоть Блокнотом) и добавьте строки вида:

    Bridge obfs4 192.0.2.34:12345 ABCDEF0123456789... cert=...
    Bridge 198.51.100.10:9001 1234567890ABCDEF...

## ⚠️ Важно:
После первого запуска **ОБЯЗАТЕЛЬНО** необходимо вручную добавить ваши мосты в файл `torrc`  
— это требуется для работы Tor в некоторых странах.  

Где получить мосты есть информация ниже.

## 🚀 Первый запуск:
При первом запуске через `start_proxy.bat` скрипт:

- Загружает **Tor Expert Bundle** (консольную сборку Tor для Windows) с офф. сайта Tor Project, через PowerShell WebClient (Можете скачать вручную).
- Распаковывает его в подпапку `tor` вашего проекта.
- Генерирует конфигурационный файл `torrc`(в него нужно добавить мосты вручную) и config.json (локальный конфиг для скрипта)
- Формирует специальную ссылку `tg://socks?server=127.0.0.1&port=9090` и открывает её в браузере 
- Если на пк установлен Telegram, он автоматически откроется с предложением добавить этот прокси в список используемых.  


    Telegram перехватывает вызов `tg://`, распознаёт параметры прокси и выводит  
    окно «Добавить прокси» — остаётся лишь подтвердить добавление.


###  Первый запуск `start_proxy.bat`:

- Проверяет наличие Python.  

- Создаёт виртуальное окружение .venv

-  Устанавливает библиотеку colorama.

- Если папка /tor в корне проекта с нужными файлами отсутствует, скачивает tor-expert-bundle через PowerShell (WebClient).

- Создаёт файл torrc с настройками (прокси на 127.0.0.1:9090), пути до файлов, флаг UseBridges 1 и шаблон мостов (добавьте их вручную)

- Формирует ссылку `tg://socks?server=127.0.0.1&port=9090` и переходит по ней, что вызывает диалог добавления прокси в Telegram.

- Если установлено десктоп-приложение Telegram - оно автоматически откроется и предложит добавить это прокси в список.

- Сохраняет все настройки в `config.json` - следующие запуски скрипта будут использовать его для работы.





## 📁 Структура проекта (после настройки)

    Telegram_tor_proxy(папка с проектом)
        ├── start_proxy.bat # Файл для быстрого запуска скрипта
        ├── start.py # "Установочный" модуль для первого запуска
        ├── tor-proxy.py # Основной модуль, отвечает за работу Tor-прокси
        ├── torrc # Конфигурация Tor (редактируется пользователем)
        ├── config.json # Локальный конфиг скрипта с путями к файлам и т.д (здесь можно сменить порт вручную.)
        │ └── ...
        ├── tor ──├ # Папка с Tor Expert Bundle (распаковать сюда, при ручной загрузке)
        │ ├── data ── ...
        │ ├── docs ── ...
        │ ├── tor ├── tor.exe
        │ └─...   ├───── pluggable_transports ── lyrebird.exe # Встроенный транспорт для мостов с обфускацией obfs4
        └─...     ├...

## ️ ⚙️ ️ А это безопасно?
`Кратко: Да`

1. Касательно Tor-соединения: Содержимое сообщений: **Безопасно**

Telegram всегда шифрует трафик между вашим устройством и своими серверами (протокол `MTProto`). 

    Даже если выходной узел Tor — «злой», он увидит только зашифрованные пакеты данных. 
    Прочитать вашу переписку, увидеть фото или узнать пароль владелец узла не сможет.

Это ключевое, что нужно знать обывателю. Остальные подробности касательно безопасности Tor-соединения рекомендую  
изучить самостоятельно если вас это интересует, здесь обсуждаться более это не будет.

2. Касательно скрипта:

- **Открытый исходный код** — весь код проекта доступен для проверки, никаких скрытых телеметрий или обращений к сторонним серверам, только к официальному сайту Tor Project при загрузке Tor Expert Bundle.
- **Локальный прокси** — SOCKS5‑порт открыт исключительно на интерфейсе `127.0.0.1`, поэтому доступ из внешней сети или других приложений невозможен без явного разрешения.
- **Никаких модификаций** — скрипт не изменяет исполняемые файлы ни мессенджера Telegram и не вмешивается в его работу, ни Tor-файлы (за исключением условно-обязательных для работы изменений в конфиге - эффект тот же, если вы сами откроете Tor Browser и будете использовать мосты), он лишь создаёт прокси и предлагает Telegram подключиться к нему стандартным способом, предусмотренным разработчиками.


### Что именно изменено в конфиге: 
Изменён порт с классического 9050 на 9090 (вероятно это бессмысленно, делалось во избежание потенциальных конфликтов,  
если скрипт одновременно используется с Tor Browser, например).  
Использовать ли мосты (флаг `UseBridges 1` (рекомендуется) и какой транспорт для мостов использовать (используется  
встроенный в tor expert bundle, `lyrebird.exe`), указание путей к файлам (нужно делать вручную для кастомного конфига,  
сами файлы никак не изменяются) и вывод логов в консоль для удобства (для первых запусков, понять устанавливает ли Tor соединение)

## ⚙️ Как это работает?

Данный скрипт использует **Tor Expert Bundle** — консольную сборку Tor, которая работает как фоновый сервис  
и предоставляет SOCKS5 ‑ прокси на выходе. Скрипт был заточен под домашнее использование в связке с **Telegram**,
но Вы можете направить через это Tor-соединение любое приложение, поддерживающее SOCKS5  
(Telegram, браузеры, консольные утилиты и т.д). Об этом подробнее ниже.

## ⚙️ Как именно Tor Expert Bundle предоставляет прокси?

Во время работы, скрипт запускает фоновый процесс `tor.exe` (из распакованного архива Tor Expert Bundle, скаченного при первом запуске).

`tor.exe` запускается с флагом `-f`+ путь до `torrc`, это заставляет читать именно этот конфигурационный файл `torrc`  
(отредактированный скриптом), в котором указано, на каком порту и адресе открыть SOCKS5 - прокси (в нашем случае 127.0.0.1:9090).  
А также другие настройки (например, использовать ли мосты (`Use Bridges 1`) и какие, и т.д) (если да - их нужно указать  
в этом файле в виде: `Bridge` `ВАШ-МОСТ`)  

После старта Tor (`tor.exe`) начинает слушать localhost на указанном порте, (от 127.0.0.1:9090) ожидая  
входящих подключений от приложений на **том же** компьютере.  

Когда какое-либо приложение (например, Telegram или браузер) хочет отправить данные через 127.0.0.1:9090,  
Tor принимает этот запрос и устанавливает соединение с целевым ресурсом.

Впоследствии весь дальнейший обмен данными между приложением и удалённым сервером проходит через этот туннель: приложение  
отправляет запросы на локальный прокси, Tor принимает их, шифрует и передаёт по своей цепочке. Ответы приходят обратно тем же путём.

### 📌 Плюсы и минусы этого метода:
Минусы этого метода  
1. Падение скорости (прямое соединение между 2-мя устройствами заменяется на цепочку соединений между (суммарно) 5-ю устройствами из разных локаций)
2. Нестабильный доступ к некоторым ресурсам (где-то принципиально ограничено Tor-соединение, а где-то, будет проблема в  
конкретной exit-node из собранной у вас цепочки соединения.
3. Проблемы с доступом к самому Tor - в некоторых странах доступ к нему изначально тоже **отсутствует**. В большинстве случаев это исправимо -  
Можно использовать мосты (ниже будет ещё раздел о мостах)

**Не стоит ожидать стабильной работы 24\7**)  
(Как минимум, придётся менять мосты время от времени.)

Плюсы:
1. Работает
2. Безопасно при обычном использовании в связке с Telegram
3. Бесплатно
4. У вас в распоряжении не просто один прокси, а каждый раз "одно из"  
(в сети Tor активно около `~2500` выходных узлов (exit nodes), и они не фиксированные. Новые появляются, старые уходят.  
Каждый раз при подключении, мы получаем одну из них - она становится нашей выходной точкой.


### Сам Tor при этом тоже ограничен в некоторых странах. 

Поэтому, подключиться к сети Tor "напрямую", например из рф (за редким исключением) невозможно.  
В качестве решения, можно использовать **мосты**.  

Какие именно использовать и где их брать - во многом, остаётся на стороне конечного пользователя (от части, надеюсь, по понятным  
причинам - но скорее всего, простые способы приведённые здесь и так окажутся рабочими и вполне достаточными для работы "из коробки"  
Ниже приведены некоторые примеры от которых можно отталкиваться, чтобы **ускорить** соединение.

### Проблема с DNS
Важный нюанс, если используете это Tor-соединение `с другими приложениями`(Не актуально для Telegram) или чем-либо ещё.  
Если приложение использует режим SOCKS5 без удалённого разрешения DNS, то запросы на определение  
IP-адреса домена могут пойти напрямую, в обход Tor — поэтому рекомендуется использовать
`SOCKS5h` или настраивать соединение так, чтобы DNS-запросы тоже шли через прокси. 

### В случае с Telegram эта проблема не возникает
Поскольку мессенджер использует внутренний механизм работы с серверами, не полагаясь на системный DNS.
Telegram использует собственную систему доменных имён (внутри MTProto), поэтому проблема DNS-утечек для него неактуальна  
— скрипт оптимизирован именно под этот сценарий. Для остальных программ рекомендуется проверять, поддерживают ли они `SOCKS5h`,  
либо дополнительно настраивать DNS-резолвинг через Tor.
️

## 🧅 Основные возможности:

- Автоматическая загрузка Tor Expert Bundle при первом запуске с офф. сайта.

- Единственная зависимость — colorama (устанавливается автоматически, в виртуальное окружение).

- Генерация конфигурационного файла torrc с необходимыми настройками.

- Генерация специальной ссылки tg://socks, которая автоматически добавляет прокси в Telegram  
(достаточно нажать «Добавить» в появившемся окне).

- Поддержка мостов (none(vanilla) и obfs4), мосты необходимо добавить вручную.

## 📌 Ручная настройка мостов:

Откройте появившийся `torrc` в корне проекта.

Добавьте строки с мостами (none(vanilla) или obfs4).

При использовании obfs4‑мостов скрипт автоматически использует `lyrebird.exe` (входит в состав Tor Expert Bundle).

## 📥 Ежедневное использование:

Запустите `tor_proxy.bat` → Tor стартует.

    (Можно добавить в автозапуск или настроить как службу)

- Добавить\Выбрать в Telegram нужный прокси.

- Всё готово. Время от времени, нужно менять мосты (старые перестают работать). Можно ориентироваться на логи - если соединение  
не устанавливается (нет `Bootstrapped 100%` - пора менять мосты)

# ❓ Часто задаваемые вопросы

### 🔹 Где взять мосты?

#### Vanilla (обычные): 
        
    https://bridges.torproject.org/options/en → выберите «None» → «Get bridges».

#### obfs4: 

1. `bridges.torproject.org` - выберите «obfs4» или кнопка `Just give me bridges!`(эта кнопка выдаёт obfs4)
2. `@GetBridgesBot` бот в Telegram - /start и далее по инструкции
3.  Email - `bridges@torproject.org` отправить письмо с почты **Gmail** или **Riseup** (посмотрите на офф.сайте подробную инструкцию)

### 🔹 Можно ли использовать этот прокси с другими программами?

`Да`. 

После запуска скрипта `start_proxy.bat` на вашем компьютере работает Tor-соединение, которое слушает адрес `127.0.0.1:9090`.  
Этот адрес можно использовать как SOCKS5‑прокси. Вы можете вручную прописать его в настройках любого приложения, поддерживающего SOCKS5.  
(Не забудьте про проблему с DNS и SOCKS5h, описанную выше, если для вас это важно).

### 🔹 Как проверить, что прокси работает?

1. Должен быть запущен `tor.exe`. Запускается сам при запуске скрипта через `start_proxy.bat`  
2. (**Bootstrapped 100%** в консоли, означает что цепочка Tor построена и соединение установлено. **Только с этого момента прокси работает**)  
3. В Telegram откройте Настройки → Продвинутые настройки → Тип соединения: Должен быть выбран **`SOCKS5`** прокси с адресом `127.0.0.1` и портом `9090`.


Также можно зайти на `check.torproject.org` через что угодно, использующее прокси `127.0.0.1:9090` для проверки.

### 🔹 Что делать, если Tor Expert Bundle не скачался автоматически?

Скрипт выведет сообщение об ошибке. В таком случае можно попробовать ещё раз либо Вы можете скачать архив вручную:  
(рекомендуется вручную, если вышла новая версия с серьёзными изменениями. Скорее всего, скрипт будет исправно работать  
после обновления, если пути к файлам не изменятся или вы разместите их вручную, как того ожидает скрипт)

   1. Перейдите на страницу загрузки Tor Expert Bundle.
   (https://www.torproject.org/download/tor/)


   2. Скачайте версию для Windows(x86_64)

      (обычно, это .tar.gz архив, открыть можно тем же win rar)


   3. Распакуйте содержимое архива так, чтобы файлы оказались **в точности** в той структуре проекта, как этого ожидает скрипт (указано выше)

Запустите `start_proxy.bat` — скрипт обнаружит файлы и продолжит работу.

### 🔹 Почему используется порт 9090, а не стандартный 9050?

Это сделано для предотвращения потенциальных конфликтов, если на компьютере уже запущен Tor Browser. (по умолчанию, Tor использует порт 9050)

Вы можете изменить порт, отредактировав переменную `SOCKS_PORT` в файле `start.py` до первого запуска  
если первый запуск был - удалите `config.json` - скрипт увидит что нет конфига и запустит процесс настройки заного,  
взяв новый порт из `SOCKS_PORT`.   
Либо измените `config.json` и `torrc` вручную.

### ❓ Вопросы / предложения?

Создавайте Issue в этом репозитории.

# ☕ Поддержать автора

Если вам помог этот проект, вы можете поблагодарить автора и поддержать дальнейшую разработку:

* ![ERC20](https://shields.io)   **USDT (ERC20 / BEP20 / Polygon):** `0xEa6B3e2b5aF6C993570403878788c32A2Be93B0a`
* ![BTC](https://shields.io)   **Bitcoin (BTC):** `bc1qv3ehcc2txuzxxfww27sxqv27prynd63s5q4c4j`

Спасибо за вашу поддержку! ❤️

